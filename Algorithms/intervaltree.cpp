#include <iostream>
#include <algorithm>

using namespace std;

int get_tree_size(int n) {
    return 1 << (int(log2(n - 1)) + 2);
}

int main() {
    int n;
    int T[1 << (int(log2(n - 1)) + 2)];


    return 0;
}